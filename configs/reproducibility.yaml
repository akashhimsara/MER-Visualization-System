project:
  name: Member1_MER
  protocol_version: v1

reproducibility:
  primary_seed: 42
  additional_seeds:
    - 123
    - 2026

dataset:
  split_level: song
  test_set_locked: true

evaluation:
  valence_arousal_separate: true

hardware:
  reference_machine: hardware_environment_v1

experiment:
  naming_convention: EXP_<FAMILY>_<NUMBER>

split:
  unit: song
  strategy: deterministic_random_song_level
  train_ratio: 0.70
  validation_ratio: 0.15
  test_ratio: 0.15
  primary_seed: 42
  test_locked: true
  split_before_windowing: true  